# Contributing

- Copy .env.example to .env and set SUPABASE_URL and SUPABASE_KEY.
- Ensure a `notes` table exists on Supabase with fields listed in README.
- Run `flutter pub get` before building.
- If analyzer flags plugin registrant differences for generated files, re-run `flutter pub get` and `flutter clean` to regenerate platform code.
